{
  "name": "pigeon-mirror",
  "version": "0.2.0",
  "private": true,
  "description": "Pigeon's mirror scraper. Runs in GitHub Actions every ~5 min, writes channel snapshots + media to the export branch.",
  "type": "module",
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "scrape": "tsx scrape.ts",
    "dry-run": "tsx dry-run.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test '*.test.ts'"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0"
  }
}
