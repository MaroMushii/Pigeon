# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editors
.vscode/
.idea/
*.swp
*.swo

# Node (mirror/)
node_modules/
*.log
npm-debug.log*

# Stale Electron-era artifacts at repo root (kept ignored to prevent
# accidental re-add). Subdirectories — mac/, mirror/ — keep their own.
/package.json
/package-lock.json
/pnpm-lock.yaml
/dist/
/node_modules/
